.DS_Store
node_modules/
dist/
*.local
.env
.env.*
!.env.example
# Not a secret — the public origin of the API, and CI has to have it or the
# build ships with a blank one and every request lands on the wrong host.
!.env.production
# Same reasoning pointed the other way: the local API's origin, fixed so that
# `npm run dev` needs no arguments and no one has to remember this port.
!.env.development
*.tsbuildinfo
