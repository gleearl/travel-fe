# The API when running `npm run dev`. `php artisan serve` defaults to this port,
# and the backend's FRONTEND_URL already names http://localhost:5173, so the
# pair lines up with no further setup.
VITE_API_URL=http://localhost:8000
