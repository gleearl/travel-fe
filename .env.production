# The API's own host. Absolute on purpose: this bundle is served from
# gleearl.github.io, so every call is cross-origin and there is no relative
# path that could reach Laravel.
VITE_API_URL=https://travel.gleearl.com
