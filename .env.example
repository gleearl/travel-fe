# Origin of the Laravel API, no trailing slash. There is no same-origin case
# here — the app is served from GitHub Pages and the API from its own host — so
# this is always set.
VITE_API_URL=http://localhost:8000
